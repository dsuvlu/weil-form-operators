# Experiment provenance

Source archive: `arithmetic_currents_experiments_precision_ladder.zip`.
Archive SHA-256: `c6df001a87a9cc85b04875c5b13d794bde8b47bd6106eabc68b19f4763ae426a`.
Archive root: `arithmetic_currents_experiments_v2/`.

All 17 Python files are preserved byte-for-byte. Only the README
and dependency specification are adapted for this standalone distribution.
No bundled plots or CSV results are presented as newly reproduced evidence.
The original archive is retained by the author; per-file hashes are supplied here.
Arbitrary precision is not certified interval arithmetic. Neither these numerical
experiments nor the formalized finite results prove the Riemann Hypothesis.

The uv environment uses the repository-root `pyproject.toml` and `uv.lock`, with
marimo in the optional `notebooks` dependency group. The original requirements
files remain compatibility snapshots. This migration changes environment setup,
not experiment or notebook calculations.
